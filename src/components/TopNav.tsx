// TopNav is now minimal - theme toggle and about moved to Sidebar
// This component can be used for page-specific actions if needed

export function TopNav() {
  // Currently empty - the equalizer is now in the PlayerOverlay
  // and theme/about are in the Sidebar
  return null;
}
